# ─────────────────────────────────────────────────────────────
# Base de datos
# ─────────────────────────────────────────────────────────────
# Ruta al fichero SQLite. El backend usa Prisma para guardar
# Links, Files, Audios y Notes del inbox.
DATABASE_URL="file:./dev.db"

# ─────────────────────────────────────────────────────────────
# Servidor
# ─────────────────────────────────────────────────────────────
# Puerto en el que escucha la API (por defecto 3001).
PORT=3001

# ─────────────────────────────────────────────────────────────
# Azure OpenAI
# ─────────────────────────────────────────────────────────────
# URL de tu recurso en Azure Portal (OpenAI).
# Ejemplo: https://mi-recurso.openai.azure.com
AZURE_OPENAI_ENDPOINT="https://hack-udc-2026.openai.azure.com/"

# Clave API del recurso (Keys en Azure Portal). No la subas a Git.
AZURE_OPENAI_API_KEY=""

# Solo para Azure OpenAI clásico (.openai.azure.com). Con Azure AI Studio/Foundry (services.ai.azure.com, api/projects) se usa la API v1 y no hace falta.
AZURE_OPENAI_API_VERSION="2024-02-15-preview"

# Modelo de chat usado para: notas, links, ficheros (texto e imágenes) y análisis de transcripciones.
# Opciones:
#   - gpt-4o        → Más capaz y con mejor visión. Más caro.
#   - gpt-4o-mini   → Más barato, también tiene visión. Recomendado para ahorrar.
# Puedes usar gpt-4o-mini para todo; el enriquecimiento seguirá funcionando bien y será más barato.
AZURE_OPENAI_DEPLOYMENT="gpt-4o"

# Modelo de transcripción de audio (solo para audios que subes al inbox).
# Whisper es el modelo de OpenAI que convierte voz → texto (speech-to-text).
# En Azure lo despliegas como un deployment aparte; aquí pones el nombre de ese deployment.
AZURE_OPENAI_WHISPER_DEPLOYMENT="whisper"